# Scraper Dockerhub Deployment

Dockerfile expects `scraper` directory in the working directory to exist.

Build with
    
    docker build . -t albsch/scraper:0.11.2

Published at

https://hub.docker.com/repository/docker/albsch/scraper
